# Database MySQL (diperlukan untuk CMS dan API)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=piam_db

# Port server backend
PORT=5000

# JWT Secret untuk autentikasi CMS (ubah di production!)
JWT_SECRET=your-secret-key-change-in-production
